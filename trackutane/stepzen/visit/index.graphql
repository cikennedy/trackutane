type Visit {
  provider: String
  date: Date
  visitDosage: Int
  cumulativeDosage: Int
  nextVisit: Date
}

type Query {
  getVisits: JSON
    @rest(
      endpoint: "https://trackutane-default-rtdb.firebaseio.com/visits.json"
    )
}
